version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    networks:
      - cineweb-network
    volumes:
      - ./src:/app/src

networks:
  cineweb-network:
    external: true
